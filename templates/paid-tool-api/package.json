{
  "name": "paid-tool-api",
  "version": "1.0.0",
  "private": true,
  "description": "A SettleMesh paid API: one metered endpoint that bills the logged-in user's Aev wallet per call.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
